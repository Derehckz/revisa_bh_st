# Boletas de Honorarios - dependencias Python
# Instalar: py -m pip install -r requirements.txt
# Versiones fijas para reproducibilidad.
#
# Python 3.14+: pandas 2.2.x no publica ruedas (wheel) para 3.14; pip intenta
# compilar desde fuente y exige Visual Studio. Usar pandas 3.x (rueda cp314).

pandas==3.0.2
openpyxl==3.1.5
rich==13.9.4
colorama==0.4.6
tqdm==4.67.0

# Windows: Outlook (envío/extracción de correos); 311 incluye rueda cp314
pywin32==311

# Base de datos y migraciones
sqlalchemy==2.0.49
psycopg[binary]==3.3.3
alembic==1.18.4
fastapi==0.115.12
uvicorn==0.34.0
python-multipart==0.0.20

# Opcionales para script 9 (revisión carpetas IP/CFT con OCR):
# PyMuPDF>=1.21.0
# pdf2image>=1.16.0
# pytesseract>=0.3.10
# Pillow>=9.0.0

# Desarrollo y tests (opcional)
pytest==8.3.3
httpx==0.28.1
