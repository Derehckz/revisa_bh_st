# QA — Épica E11 — SyncProjector + frescura

## Alcance
- `lib/sync_projector.py`: `apply_hints` recalcula sync; opcional `ensure_periods_from_disk`
- `GET /operations/period/sync-status?refresh=true` → proyector
- Query API: `GET /period/{y}/{m}` incluye `data_freshness`
- UI Operación: botón **Re-sync período**; Dashboard muestra frescura si ≠ ok

## CLI
- [ ] N/A (no toca etapas CLI)

## API
- [ ] `GET /operations/period/sync-status?year=2026&month=Julio`
- [ ] `…&refresh=true` puede devolver `periods_created`
- [ ] `GET /period/2026/Julio` incluye `data_freshness.status`
- [ ] PG caído → `unknown`, no 500

## Frontend
- [ ] Operación: botón Re-sync; toast con resultado
- [ ] Dashboard: aviso si `data_freshness` ≠ ok
- [ ] Operación sigue usable con badge degraded

## Excel / Outlook / PostgreSQL
- [ ] Excel: lectura solo para conteo
- [ ] Outlook: N/A
- [ ] PG: ensure períodos solo con refresh

## Regresión / Borde
- [ ] Sin API key → 401
- [ ] refresh no bloquea pipeline

## Automatizado
- [ ] `pytest tests/test_sync_status.py tests/test_sync_projector.py -q`

## Veredicto
PASS si checklist OK
